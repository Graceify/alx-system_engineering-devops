#!/bin/bash

# Store the current working directory in a variable
previous_dir=$(pwd)

# Change the directory to the previous one
cd ..

# Verify the current working directory
echo "Current directory: $(pwd)"

# Change back to the previous directory
cd "$previous_dir"

# Verify the current working directory after changing back
echo "Current directory after change back: $(pwd)"

